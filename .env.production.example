DOMAIN=bot.example.com
CONTACT_EMAIL=you@example.com
# Optional connection from the existing hosted MEME LAB UI. Generate a random
# token locally; put the same value in Site REMOTE_ENGINE_TOKEN (server secret).
REMOTE_API_TOKEN=
ALLOWED_RPC_HOSTS=
