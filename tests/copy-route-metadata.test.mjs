import test from 'node:test';import assert from 'node:assert/strict';
import {BSC} from '../core/copy/common.mjs';
import {poolLookupTtl,localV2AmountOut,restoreRouteMetadata} from '../runtime/copy/route-metadata.mjs';
import {PaperDexRoute} from '../runtime/copy/paper-dex-route.mjs';
import {PancakeV2Route} from '../runtime/copy/routes.mjs';
import {candidatePriority} from '../runtime/copy/quote-selection.mjs';
const word=x=>'0x'+BigInt(x).toString(16).padStart(64,'0');
test('only positive canonical factory lookups get long cache TTL',()=>{assert.equal(poolLookupTtl(BSC.factory,word(1)),86400000);assert.equal(poolLookupTtl(BSC.factory,word(0)),10000);assert.equal(poolLookupTtl('0x'+'11'.repeat(20),word(1)),10000);assert.equal(poolLookupTtl(BSC.factory,'0x'),10000);});
test('authenticated pool metadata is shared by concurrent route adapters and survives restart',async()=>{let reads=0;const address='0x'+'12'.repeat(20),token='0x'+'34'.repeat(20);const rpc={cache:new Map(),contract:async(_to,_abi,m)=>{reads++;return [{token0:BSC.wbnb,token1:token,factory:BSC.factory,getPair:address}[m]];}};let saved;const store={setting:async()=>saved,set:async(_k,v)=>{saved=v;}};await restoreRouteMetadata(rpc,store);const a=new PaperDexRoute(rpc),b=new PaperDexRoute(rpc);await Promise.all([a.pool({address}),b.pool({address})]);assert.equal(reads,4);await rpc.flushRouteMetadata();const restarted={cache:new Map(),contract:()=>{throw Error('must use verified immutable metadata');}};await restoreRouteMetadata(restarted,store);assert.equal((await new PaperDexRoute(restarted).pool({address})).token1,token);assert.equal(saved.pools.length,1);});
test('nonexistent Pancake pools do not invoke reverting amount quotes',async()=>{let quotes=0;const rpc={contract:async(_t,_a,m)=>{if(m==='getAmountsOut'){quotes++;throw Error('unexpected');}if(m==='getPair')return ['0x'+'00'.repeat(20)];throw Error(m);},call:async()=>word(1)};const r=new PancakeV2Route(rpc);r.verifiedAt=Date.now();await assert.rejects(r.quote({side:'BUY',token:'0x'+'56'.repeat(20),amount:'100',slippageBps:100}),/NO_PANCAKE/);assert.equal(quotes,0);});
test('V2 local arithmetic uses exact integer 0.25 percent fee and floors output',()=>{assert.equal(localV2AmountOut(1000,100000,200000),1975n);assert.equal(localV2AmountOut(1,100000,200000),1n);assert.throws(()=>localV2AmountOut(1,0,2),/INVALID/);});
test('observed pool authentication retains urgent priority and research stays in background',()=>{const p={id:'PANCAKE_SMART',authenticatesObservedPools:true},r={paper:true,candidatePools:[{}]};assert.equal(candidatePriority(p,r,2),2);assert.equal(candidatePriority(p,r,-1),-1);});
